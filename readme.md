#Instodo - AngularJS version

This was initially implemented in jQuery. This branch is the AngularJS version. It uses...

* angular-ui-tree for sorting/dragging
* elastic.js for textareas that expand with input